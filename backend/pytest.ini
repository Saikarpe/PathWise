[pytest]
# `.` on the path so `import app...` resolves when pytest is run from
# backend/ without setting PYTHONPATH by hand.
pythonpath = .
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
